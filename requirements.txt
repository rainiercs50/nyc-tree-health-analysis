# NYC Street Tree Health Predictor — dependencies
# scikit-learn is pinned: the saved .joblib models were trained on 1.7.2,
# and loading them under a different version will warn / may break.
streamlit>=1.33
pandas>=2.0
numpy>=1.26
scikit-learn==1.7.2
joblib>=1.3
plotly>=5.18
matplotlib>=3.7
